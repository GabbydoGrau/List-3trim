// algoritmo que lê dois valores...
let a = prompt("Digite o valor de a:");
let b = prompt("Digite o valor de b:");

if (a % b === 0 || b % a === 0) { //pra verificar se são multiplos? 
console.log(${a} e ${b} 'são múltiplos');
} else {
console.log(${a} e ${b} 'não são múltiplos'); //eu não consigo imprimir com os valores 
}
